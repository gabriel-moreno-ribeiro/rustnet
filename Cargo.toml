[package]
name = "rustnet"
version = "1.0.0"
edition = "2021"
description = "A userspace TCP/IP stack written from scratch, running on a TUN device"
license = "MIT"

[dependencies]

[lib]
name = "rustnet"
path = "src/lib.rs"

[[bin]]
name = "rustnet"
path = "src/main.rs"
